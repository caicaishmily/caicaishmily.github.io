language: node_js
sudo: required
node_js:
  - 10.9.0
cache:
  directories:
    - node_modules
install:
  - npm install -g codecov

script:
  - istanbul cover ./node_modules/mocha/bin/_mocha --reporter lcovonly -- -R spec
  - codecov
  - sh ./deploy.sh
branch: dev
